import React from "react";

import { {{ pascalCase name }} } from "../components";

export default { title: "Components/{{ pascalCase name }}", component: {{ pascalCase name }} };

export const Basic = (): JSX.Element => {
  return <{{ pascalCase name }} />;
};
